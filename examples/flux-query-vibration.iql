from(bucket: "vtrFeederData")
  |> range(start: v.timeRangeStart, stop: v.timeRangeStop)
  |> filter(fn: (r) => r["_measurement"] == "status_data")
  |> filter(fn: (r) => r["_field"] == "VibrationDataFloats[0].VibrationX")
  |> aggregateWindow(every: 1m, fn: mean, createEmpty: false)
  |> mean()  // Aggregate all results into a single mean value
  |> yield(name: "mean")
