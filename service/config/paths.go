// file: service/config/paths.go
package config

// SharedDir defines the relative path to the shared directory, which contains
// configuration files like architect.yaml and is used for CSV uploads.
const SharedDir = "../shared"
